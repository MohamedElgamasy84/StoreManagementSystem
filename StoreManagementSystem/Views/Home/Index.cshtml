@{
    ViewBag.Title = "الرئيسية";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container text-center mt-5">

   

    <div class="d-flex flex-column align-items-start p-3" style="position:fixed; right:0; bottom:5px;">
        <span style="color: orangered">@DateTime.Now.ToString("yyyy-MM-dd")</span>
        <span style="color: brown">@DateTime.Now.ToString("hh:mm tt")</span>
    </div>



    @* <div class="d-flex flex-column align-items-start p-3" style="position:fixed; right:0; top:50px;">
        <a href="@Url.Action("Index", "Home")" class="btn btn-outline-primary my-1 w-100 text-start">الرئيسية</a>
        <a href="@Url.Action("Index", "Item")" class="btn btn-outline-secondary my-1 w-100 text-start">الاصناف</a>
        <a href="@Url.Action("Index", "Supplier")" class="btn btn-outline-secondary my-1 w-100 text-start">الموردين</a>
        <a href="@Url.Action("Index", "Customer")" class="btn btn-outline-secondary my-1 w-100 text-start">العملاء</a>
        <a href="@Url.Action("Index", "User")" class="btn btn-outline-secondary my-1 w-100 text-start">المستخدمين</a>
        <a href="@Url.Action("Settings", "Home")" class="btn btn-outline-secondary my-1 w-100 text-start">الإعدادات</a>
    </div> *@

</div>